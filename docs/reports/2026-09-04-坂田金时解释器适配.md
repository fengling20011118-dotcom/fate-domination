# 坂田金时解释器适配检查点

日期：2026-09-04。状态：角色切片档案修正完成，三张技能已通过共享解释器自动执行入口。

## 1. 产物与范围

- 人工维护档案：`data/authoring/servants/servant.kintoki.json`。
- 机器报告：`artifacts/ability-interpreter/kintoki-adapter-report.json`。
- 行为测试：`packages/rules/tests/kintoki-authoring.test.ts`。
- 原始 HTM：`chm-extract/坂田金时.htm`。

本次处理坂田金时角色切片，并补齐共享解释器对其所需的通用机制：技能区魔力例外、局势禁止例外、每局一次、移除区选牌、可选支付、跨决策原子结算和移动结果计数。

## 2. 核对结论

主仓库本地快照协调结果确认：坂田金时 canonical ID 为 `servant.kintoki`。三张技能顺序为：

| 顺序 | canonical ID | 名称 | 费用 | 威力 | 状态 |
|---:|---|---|---:|---:|---|
| 1 | `servant.kintoki.skill.sc-kintoki-1` | 黄金冲击 | 0 | 11 | automatic |
| 2 | `servant.kintoki.skill.sc-kintoki-2` | 黄金冲击 | 0 | 11 | automatic |
| 3 | `servant.kintoki.skill.sc-kintoki-3` | 黄金噬者 | 5 | 0 | automatic |

现有 authoring 文件原先只有两张技能，且把黄金噬者放在 `sc-kintoki-1`，黄金冲击放在 `sc-kintoki-2`。本次已按 canonical 顺序改正，并补回第二张黄金冲击。

## 3. HTM 与图片证据

`chm-extract/坂田金时.htm` 的图片顺序为：

1. `ScreenShot_2025-11-02_112043_708.png`：从者概览。
2. `ScreenShot_2025-11-02_112048_572.png`：黄金噬者。
3. `ScreenShot_2025-11-02_112053_949.png`：黄金冲击。
4. `ScreenShot_2025-11-02_112053_949.png`：与第 3 张重复引用。

第 3、4 张图片重复是页面证据问题，不代表只有一张黄金冲击。本次在 authoring 的 `sources` 和两张黄金冲击的 `evidence` 中保留了 `htmImageIndex` 与 `duplicateOfHtmImageIndex`，并按协调者结论保留两张独立技能牌。

## 4. 结构化与解释器状态

黄金冲击保留以下语义：

- 【真名解放】：支持为 `declaration_reveal`，可通过正式 `play_card` 入口触发。
- 你拥有的魔力少于 8 点也可打出此牌：出牌合法性入口读取本牌规则例外，只对本牌放行技能区魔力门槛。
- 局势牌无法禁止你打出此牌：出牌合法性入口读取本牌局势禁止例外。
- 每局游戏限一次：正式出牌入口按卡牌实体记录本局使用次数，第二次打出同一实体会拒绝。

黄金噬者保留以下语义：

- 【真名解放】：已支持为 `declaration_reveal`。
- 战斗阶段从移除游戏区将一张黄金冲击加入技能区。
- 可以花费 7 魔力将另一张黄金冲击也加入技能区。
- 位于战场时，每加入一张黄金冲击获得 2 VP。

黄金噬者的移除区检索、技能区添加、第二张黄金冲击可选支付 7 魔力、本次实际加入数量计数和战场 VP 奖励均已通过共享解释器执行。目标选择在结算前完成，支付、移动和 VP 奖励作为同一正式命令链原子提交。

## 5. 已补齐的共享机制

| 最小复现 | 当前机制 | 共享文件 |
|---|---|---|
| 玩家在技能区有 `sc-kintoki-1`，魔力低于 8，局势禁止出牌仍尝试打出黄金冲击 | 出牌合法性入口读取卡牌级 ruleModifier，允许此牌无视技能区 8 魔力门槛和局势禁止 | `packages/rules/src/ability/interpreter.ts`、`packages/rules/src/ability/loader.ts` |
| 黄金冲击首次打出后再次尝试使用同一实体牌 | 每局一次使用限制由后端权威记录并阻止第二次 | `packages/rules/src/ability/interpreter.ts`、`packages/rules/src/ability/loader.ts` |
| 黄金噬者已激活，战斗阶段从 removed_from_game 找回黄金冲击 | 从移除游戏区按 canonical ID 选择并移动到技能区 | `packages/rules/src/ability/interpreter.ts`、`packages/rules/src/ability/loader.ts` |
| 黄金噬者可选支付 7 魔力加入第二张黄金冲击 | 后端提供可选目标选择；选择第二张时支付 7 魔力，支付与移动原子结算 | `packages/rules/src/ability/interpreter.ts`、`packages/rules/src/ability/loader.ts` |
| 黄金噬者在战场加入 1 或 2 张黄金冲击 | 结算上下文提供本次实际加入数量，并据此奖励 `2 * count` VP | `packages/rules/src/ability/interpreter.ts`、`packages/rules/src/ability/loader.ts` |

2026-09-04 用户确认：第二张黄金冲击采用独立支付窗口；该功能等价于额外花费 7 点魔力加入一张技能卡。VP 奖励计数优先由通用移动卡牌结果统计本次实际加入技能区的黄金冲击数量。

## 6. 验证记录

- `npm test -- packages/rules/tests/kintoki-authoring.test.ts`：退出码 1，RED，确认旧档案只有两张技能且 `sc-kintoki-3` 缺失。
- `npm test -- packages/rules/tests/kintoki-authoring.test.ts`：退出码 0，5/5 通过。
- `npx tsx scripts/report-authoring-abilities.ts data/authoring/servants/servant.kintoki.json artifacts/ability-interpreter/kintoki-adapter-report.json`：退出码 0，生成报告成功。

机器报告摘要：3 张牌、10 条 ability、3 张完整可执行牌、10 条 automatic 节点、0 条 unsupported、0 条 text_unconfirmed。

## 7. 待确认项

当前无待确认项。坂田金时两个运行时口径已在 2026-09-04 确认：第二张黄金冲击走独立支付窗口；VP 奖励数量由通用移动卡牌结果统计。
