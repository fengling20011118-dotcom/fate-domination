- 2026-09-11 持久任务 tsk_ac9ff73f63226d5d 恢复检查：以当前工作树为准，继续推进 PARTIAL=0。下一批目标为 Ruler Seal 公共机制复审（景虎/奥伯龙相关技能），不回滚已有修改。
# 重构迁移状态

## 独立战斗与牌生命周期技能批次（2026-09-01）

- 完成伊斯坎达尔【王之军势】、冲田总司【无明三段突】、巴御前【真言·圣观世音菩萨】、贞德【红莲圣女】、卫宫切嗣【四倍速】、尼禄·卡欧斯【哨兵】、贞德〔Alter〕【咆哮吧，吾之风怒】和茨木童子【沸腾之血】共 8 项技能。
- 新增临时攻击批次、同战场对手威力修正、全体战斗参与者败北、攻击关闭与暗置牌激活、地利翻倍、攻击数量比较以及魔力门槛战果扣除等结构化规则。
- 【沸腾之血】的“魔力少于 8 点才可打出”已进入统一卡牌合法性入口，没有只实现战斗能力后误标完成。
- 当前技能审计目标为 `FULL 186 / PARTIAL 758 / MANUAL 0 / DISABLED 0`。

## 技能规则程序审计分解（2026-09-01）

- 扩展 `npm run audit:skills` 输出并写入 `docs/skill-audit.json`：943 项技能均有规则程序，178 项可执行 `FULL`，766 项保持 `PARTIAL`。
- 审计按规则节点统计：14 个确定性效果节点、2304 个未解析节点；未解析原因分为选择 359、条件 538、目标 245、生命周期 470、独特规则 692。
- 新增回归测试，确保审计报告与规则程序数量一致，并保留所有未解析原因；没有将资料不足或复杂技能提升为 `FULL`。
- `npm test`：362 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试全部通过。
- `npm run validate`：通过（4 个地图地点、68 名御主、183 名从者）。
- `npm run audit:skills`：通过；静态 943、运行时 944，`FULL 186 / PARTIAL 758 / MANUAL 0 / DISABLED 0`；无 FULL 缺失处理器、无非 FULL 错误绑定处理器。

## 全量技能规则程序编译（2026-09-01）

- 新增统一技能规则程序 `SkillRuleProgram`，一次覆盖全部 943 项技能。
- 已识别的确定性效果保留为 `effect` 节点；选择、条件、目标、生命周期和独特规则保留为带原因的 `unresolved` 节点，原文与条款索引不丢失。
- 新增 `npm run migrate:skills` 一键入口，连续生成规则程序、全量迁移队列和每批最多 100 项的迁移记录。
- 规则解释器入口拒绝仍含 `unresolved` 节点的程序，避免把结构化记录误当成完整执行逻辑。
- 本次 `npm test`：350 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试，全部通过。
- `npm run validate`：通过（4 个地图地点、68 名御主、183 名从者）。
- `npm run audit:skills`：通过；静态技能 943、运行时技能 944，`FULL 178 / PARTIAL 766 / MANUAL 0 / DISABLED 0`。

## skills-005 子技能段落结构化补全（2026-09-01）

- 为技能条款新增 `sectionName` 元数据，识别开发版牌面明确写出的“标题-阶段/关键词”子技能段落（例如“审判-战斗阶段”“炽烈燃烧-被动/行动阶段”）。
- 仅补充迁移追踪、审计和公开描述所需的结构，不执行中文文本，也不改变 `FULL/PARTIAL/MANUAL/DISABLED` 等级。
- 更新 `docs/skill-migration-batches.json` 的批次记录并新增 1 项解析回归测试。
- `npm test`：349 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试，全部通过。
- `npm run validate`：通过（4 个地图地点、68 名御主、183 名从者）。
- `npm run audit:skills`：通过；静态技能 943、运行时技能 944，`FULL 178 / PARTIAL 766 / MANUAL 0 / DISABLED 0`。
- `git diff --check`：通过；仅有 Git 的换行格式提示，无内容错误。

## skills-004 批次审查与确定性效果入口（2026-09-01）

- 按批次工作集审查 `skills-004` 的 100 项，逐项保留运行时支持等级、已解析效果数量、未解析条款数量，以及选择、衍生牌、生命周期和战斗特征。
- 扩展技能文本结构化解析：识别顶层独立条款、真名解放/限次元数据和“打出时/被动/阶段”前缀；解析器仍不执行中文文本。
- 新增统一 `core.parsed-effects` 处理器入口，但只有完整解析且无条件、无选择、无目标、无生命周期的效果才允许自动使用；本批复杂技能仍保持 `PARTIAL`，没有为了凑批次提升等级。
- 新增 `docs/skill-batch-review-004.json` 和回归测试，确保 100 项审查记录与运行时定义一致。
- `npm test`：341 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试，全部通过。
- `npm run validate`：通过（4 个地图地点、68 名御主、183 名从者）。
- `npm run audit:skills`：通过；静态技能 943、运行时技能 944，`FULL 173 / PARTIAL 771 / MANUAL 0 / DISABLED 0`。

## skills-003 验证修正与批次同步（2026-09-01）

- 同步【炼金术师】新增处理器后的支持等级断言：静态技能 `FULL 172 / PARTIAL 772`，运行时技能仍为 944 项。
- 修正【炼金术师】回归场景使用第一回合 `round = 1`，来源实例 ID 与规则回合一致。
- 重新生成技能批次记录；既有 11 批、每批不超过 100 项，批次成员不因支持等级变化而重排。
- `npm test`：339 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试，全部通过。
- `npm run validate`：通过（4 个地图地点、68 名御主、183 名从者）。
- `npm run audit:skills`：通过；静态技能 943、运行时技能 944，`FULL 172 / PARTIAL 772 / MANUAL 0 / DISABLED 0`。

## 共享牌生命周期字段批次（2026-09-01）

- 为【厄运】补齐开发版明确的 `returnToDeckOnDefeat: true`，并同步技能定义、正式技能卡目录和迁移批次契约。
- 该字段复用既有牌库生命周期处理器，确保战败后按规则洗回牌库；技能其余“败北时可弃置并令同场对手败北”等效果仍保持 `PARTIAL`。
- `npm test`：340 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试，全部通过。
- `npm run validate`、`npm run audit:skills`、`git diff --check`：通过（审计仍为 `FULL 172 / PARTIAL 772 / MANUAL 0 / DISABLED 0`）。

## skills-002 完整被动处理器（2026-09-01）

- 完成琥珀【笑面琥】的两段明确效果：游戏开始将 2 张【魔力猛攻】加入个人牌库；每个准备阶段抽牌后将手牌补至 4 张。
- 新增 `core.kohaku-smile` 处理器，分别响应 `game.started` 与 `round.started`，并保持标准模式限定；技能状态由 `PARTIAL` 提升为 `FULL`。
- 新增处理器回归场景，确认牌库来源、幂等加入和准备阶段补牌行为。
- `npm test`：341 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试，全部通过。
- `npm run validate`、`npm run audit:skills`、`git diff --check`：通过；当前审计为 `FULL 173 / PARTIAL 771 / MANUAL 0 / DISABLED 0`。

## 共享出牌触发批次（2026-09-01）

- 批量登记开发版明确的“打出时抽一张牌”结构：贝亚德、厄运、虚数环 3 项均写入 `drawOnPlay: 1`，并同步到对应 `card.skill.*` 目录项和迁移批次契约。
- 这些技能仍保留 `PARTIAL`，因为同一张牌还包含移动、败北、追加出牌或牌库交换等未完成效果；不会因单个抽牌片段而伪装为 `FULL`。
- 新增内容和批次契约回归测试；本批测试通过后再继续下一组规则明确的共享机制。

## skills-003 批量结构化补全（2026-09-01）

- 按整批处理 `skills-003` 的 100 项，不再按单个技能停顿；本批补齐开发版文本中可直接证明的牌面约束：少于 8 点魔力可用、追加出牌、单卡出牌和每回合一次限制。
- 约束只进入统一内容导入层，未改变效果处理器，也未将 `PARTIAL` 技能伪装为 `FULL`。复杂效果继续保留原文和 `PARTIAL` 状态。
- 新增内容回归断言，覆盖佐佐木小次郎、卫宫（村正）、乔治和伊莉雅等批次样例，并重新生成批次契约。
- `npm test`：338 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试，全部通过。
- `npm run validate`：通过（4 个地图地点、68 名御主、183 名从者）。
- `npm run audit:skills`：通过；静态技能 943、运行时技能 944，`FULL 171 / PARTIAL 773 / MANUAL 0 / DISABLED 0`。本批是结构化迁移，不代表 100 项全部达到 `FULL`。

## skills-002 实际迁移组（2026-09-01）

- 从 `skills-002` 开始完成 3 项规则明确的开局被动：卫宫士郎【投影】、远坂时臣【元素使】、间桐脏砚【五百年的活尸】。三项均接入幂等的 `core.game-start-add-skill` 处理器，并保留开发版文本来源；审计状态由 `FULL 165 / PARTIAL 779` 更新为 `FULL 168 / PARTIAL 776`。
- 补齐开发版卡面中未出现在 `data_cards.js` 的两张正式衍生卡定义：【干将·莫邪】（卫宫士郎）和【火炎弹】（远坂时臣）。两张牌拥有稳定 ID、归属、费用、属性、门槛、文本和开发版来源；复杂的卡面后续效果仍保持 `PARTIAL`，没有伪装成 `FULL`。
- 重新生成 `src/content/generated/legacy-content.json`、开发版图片来源索引、英文版从者来源索引和技能批次记录；正式卡牌定义数由 1028 增至 1030，技能批次仍为 11 批（每批最多 100 项）。
- 回归验证：`npm test` 333 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试全部通过；`npm run validate` 通过；`npm run audit:skills` 通过（静态技能 943、运行时技能 944、FULL 168、PARTIAL 776、MANUAL 0、DISABLED 0）。

## 批次覆盖修正（2026-09-01）

- 修正 `tools/build-skill-migration-batches.mjs`：一次运行会连续补齐所有尚未进入批次的技能，每批仍限制为最多100项，不再只追加一个批次后留下未分配尾部。
- 迁移记录由原来的925项扩展为全部943项，新增 `skills-010`（100项）和 `skills-011`（18项）；本次仅补齐结构化迁移追踪，不改变技能执行等级，当前审计仍为 `FULL 165 / PARTIAL 779 / MANUAL 0 / DISABLED 0`。
- 新增回归测试，验证批次ID集合与运行时943项技能完全一致且不重复。
- `npm test`：333项 JavaScript/场景测试 + 3项 TypeScript 核心测试全部通过。

## 批量技能迁移（2026-09-01）

- 本轮继续完成 `skills-002` 的第二个通用被动处理器：伊莉雅【小圣杯】在标准模式下使攻击费用每张减少1点；若本回合确实出过攻击且全部暗置，回合结束获得1点魔力，并清理本回合标记。费用计算、常规出牌和效果追加出牌共用同一标记；新增费用与回合奖励回归测试。`npm test`：332 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试全部通过；`npm run validate`、`npm run audit:skills` 和 `git diff --check` 均通过。当前审计：`FULL 165 / PARTIAL 779 / MANUAL 0 / DISABLED 0`。

- 本轮从 `skills-002` 开始实现规则处理器：伊莉雅【天之衣】已接入 `combat.resolved → round.ended → round.started` 事件链，只有上一回合共同战斗且在高潮淘汰名单中的玩家才会在下一回合开始让伊莉雅获得2点战果；新增正向与负向回归场景。`npm test`：331 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试全部通过；`npm run validate`、`npm run audit:skills` 和 `git diff --check` 均通过。当前审计：`FULL 164 / PARTIAL 780 / MANUAL 0 / DISABLED 0`。

- 本轮按“整批 100 项”完成 `skills-002` 的完整技能契约迁移记录：批次记录现在同步保留属性、规则门槛、能力费用、使用限制、唯一组、真名条件、激活条件、处理器、实现等级、能力列表和原始文本，并通过运行时定义逐项一致性回归测试。此轮只补齐可追踪的结构化契约，不把没有专用处理器的技能提升为 `FULL`。`npm test`：329 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试全部通过；`npm run validate`、`npm run audit:skills` 和 `git diff --check` 均通过。当前审计：`FULL 163 / PARTIAL 781 / MANUAL 0 / DISABLED 0`。

- 本轮验证修正已完成：`skills-001` 的 100 项登记记录与运行时支持等级重新同步，巴泽特【时间迷失】、【再启动】、【觉醒】均保留 `FULL` 和统一处理器；首日测试场景按第二日清除日数标记。当前 `npm test` 为 327 项 JavaScript/场景测试 + 3 项 TypeScript 核心测试，全部通过；`npm run validate`、`npm run audit:skills`、`git diff --check` 全部通过。当前审计：`FULL 161 / PARTIAL 783 / MANUAL 0 / DISABLED 0`。需要明确：`skills-001` 的 100 项是本批内容登记/追踪上限，其中 37 项已完成可执行处理器，63 项仍为 `PARTIAL`；不能将批次登记数量当作 `FULL` 数量。

- 本轮完成 `skills-001` 的 4 项独立技能处理器：吉尔伽美什【天地乖离开辟之星】、卫宫切嗣【起源弹】、戈尔德鲁夫【铁腕绅士】、卫宫士郎【远离尘世的理想乡】。修正铁腕绅士开局重复触发的幂等性，并补齐高潮淘汰回归场景。`npm test`：322 项 JavaScript/场景测试与 3 项 TypeScript 核心测试全部通过；`npm run validate`、`npm run audit:skills` 与 `git diff --check` 均通过。当前审计为 `FULL 154 / PARTIAL 790 / MANUAL 0 / DISABLED 0`，`skills-001` 为 `FULL 35 / PARTIAL 65`。
- 随后完成 `skills-001` 的 2 项规则明确被动/阶段能力：丹·布莱克莫尔【荣誉】（恰有两名对手的战场胜利时移除竞争战果）与哈桑【妄想心音】（隐藏真名战斗胜利时追加本回合战果，终局单挑处理对手）。新增 2 项回归测试；当前预期审计为 `FULL 156 / PARTIAL 788 / MANUAL 0 / DISABLED 0`，`skills-001` 为 `FULL 37 / PARTIAL 63`。

- 本轮继续完成 `skills-001` 的 3 项规则明确技能：莱昂纳多【生而为王】（连续战斗胜利的下一回合合计威力加成）、远野志贵【贫血症】（上回合争夺战后的下一回合威力惩罚）、菲奥蕾【回路不良】（非高潮/高潮回合每回合总魔力获得上限）。新增 `combatHistory` 结构字段、历史战斗被动处理器及统一回合魔力上限；新增 3 项回归测试。`npm test`：313 项 JavaScript/场景测试与 3 项 TypeScript 核心测试全部通过；`npm run validate`、`npm run audit:skills` 均通过。当前审计为 `FULL 144 / PARTIAL 800 / MANUAL 0 / DISABLED 0`，`skills-001` 为 `FULL 25 / PARTIAL 75`。

- 新增 `npm run batch:skills`，按固定批次生成每批最多 100 项技能；当前已固定 `skills-001` 至 `skills-009`，共 825 项结构化迁移记录（8 批各 100 项，最后一批 25 项），按共同结构分组并以原始内容顺序稳定排列。
- `skills-001` 已开始真实执行迁移：混沌【恐惧】的“支配”绑定同战场【幸运】攻击归零处理器，并补充战斗阶段回归测试；该项由 `PARTIAL` 升级为 `FULL`。
- 本轮继续完成 `skills-001` 中阿尔托莉雅与亚瑟的【风王结界】：复用力量属性归零处理器；阿尔托莉雅真名隐藏时费用减少2，亚瑟开局锁定真名不可解放。新增费用、战斗目标和真名锁定回归测试；`npm test`：298 项 JavaScript/场景测试与 3 项 TypeScript 核心测试全部通过；`npm run validate` 与 `npm run audit:skills` 均通过。当前审计为 `FULL 128 / PARTIAL 816 / MANUAL 0 / DISABLED 0`，`skills-001` 为 `FULL 9 / PARTIAL 91`。
- 随后完成混沌【猎手】：按同战场对手人数为本次战斗增加合计威力，并在回合结束清理回合标记。新增战斗威力与清理回归测试；`npm test`：299 项 JavaScript/场景测试与 3 项 TypeScript 核心测试全部通过；`npm run validate` 与 `npm run audit:skills` 均通过。当前审计为 `FULL 129 / PARTIAL 815 / MANUAL 0 / DISABLED 0`，`skills-001` 为 `FULL 10 / PARTIAL 90`。
- 每项记录保存稳定 ID、归属、阶段窗口、费用、来源引用、逐句结构分段、可解析效果、未解析条款和下一步处理器要求；文件为 `docs/skill-migration-batches.json`。
- `STRUCTURED` 仅表示内容和文本结构已经迁移、可追踪，绝不等同于可执行；正常对局仍只开放拥有已注册处理器的 `FULL` 技能。
- 当前批次统计：`skills-001` 为 37 项 `FULL` / 63 项 `PARTIAL`，`skills-002` 至 `skills-008` 各为 0 项 `FULL` / 100 项 `PARTIAL`，`skills-009` 为 0 项 `FULL` / 25 项 `PARTIAL`；批次登记数量不等于可执行覆盖率。`skills-001` 的 100 项内容已全部登记，剩余 63 项需按规则证据补处理器，不以批次登记数冒充 `FULL`。

## 全量迁移队列（2026-09-01）

- 新增 `npm run queue:skills`，从开发版内容快照生成可追踪的全量技能迁移队列。
- 队列当前包含 943 项技能，其中 795 项为 `PARTIAL`，按激活类型、阶段窗口、衍生牌、玩家选择和生命周期特征分为 113 组；当前审计为 `FULL 149 / PARTIAL 795 / MANUAL 0 / DISABLED 0`。
- 队列生成只建立迁移顺序和证据索引，不改变技能结算，也不会把 `PARTIAL` 伪装成 `FULL`。
- 技能定义与正式卡牌契约新增结构化 `effects` / `unparsedEffects` 字段；当前仅将规则可直接确认的无条件片段纳入结构化效果，条件和复合效果继续保留原文等待专用处理器。

## 最近阶段验证（2026-09-01）

- 本轮继续完成 `skills-001` 的 5 项规则明确技能：菲奥蕾【温顺】（低战果对手时合计威力-2，禁宝具局势下其技能牌威力归零）、莱昂纳多【帝王之气】（所在战场事件战果+1）、唐吉诃德【流浪骑士的大冒险】（按事件印刷战果调整个人奖励）、阿尔托莉雅【誓约胜利之剑】（高潮回合合计威力+4，第11回合胜利触发即时胜利）。新增战斗威力、事件奖励和第11回合胜利回归测试；`npm test`：318 项 JavaScript/场景测试与 3 项 TypeScript 核心测试全部通过；`npm run validate`、`npm run audit:skills` 均通过。当前审计为 `FULL 150 / PARTIAL 794 / MANUAL 0 / DISABLED 0`，`skills-001` 为 `FULL 31 / PARTIAL 69`。

- 完成开发版内容的第一阶段完整登记：68 名御主、183 名从者、347 项御主技能、596 项从者技能、13 张局势牌、1 个 20 张事件组、2 张文明废墟事件，以及 1028 个正式卡牌定义。
- 为全部 943 项技能生成独立 `card.skill.*` 卡牌目录项，并通过 `linkedSkillId` 关联可执行技能定义；已有基础卡牌 ID 保持不变。
- 技能卡目录项默认标记为 `PARTIAL`，不会自动获得可执行处理器；现有 `FULL/PARTIAL/MANUAL/DISABLED` 技能等级不因内容登记改变。
- 修正开发版卡图与 CHM 来源索引的合并策略：保留开发版来源，并为每项技能保留一个备用来源，避免重新导入后覆盖追溯信息。
- `npm test`：本阶段新增间桐慎二【无用之人】开局加入【伪臣之书】与技能卡契约同步后，共 281 项 JavaScript/场景测试与 3 项 TypeScript 核心测试通过；技能审计基线为 `FULL 108 / PARTIAL 836 / MANUAL 0 / DISABLED 0`。
- 本批完成技能卡契约投影：注册表为全部 943 项技能生成独立正式卡牌结构，补齐 `version`、`cardType`、`ownerDefinitionId`、`linkedSkillId`、阶段窗口、步骤窗口、实现等级、处理器和来源引用；不改变技能支持等级或执行规则。新增全量契约一致性回归测试。`npm test`：281 项 JavaScript/场景测试与 3 项 TypeScript 核心测试全部通过；`npm run validate` 与 `npm run audit:skills` 均通过，审计仍为静态 943、运行时 944，`FULL 108 / PARTIAL 836 / MANUAL 0 / DISABLED 0`。
- 本批同步正式卡牌目录与技能注册表：所有 `card.skill.*` 目录项现在复用关联技能的归属、属性、阶段/步骤窗口、使用限制、真名条件、实现等级、处理器和来源引用，避免客户端读取目录时与规则引擎看到不一致的支持状态。新增目录—技能一致性回归断言。第二次 `npm test`：281 项 JavaScript/场景测试与 3 项 TypeScript 核心测试全部通过；阶段级 `npm run validate`、`npm run audit:skills` 与 `git diff --check` 均通过。
- 本次 `skills-001` 处理器迁移：`master.chaos.skill.s9`（混沌【恐惧】）使用专用 `core.chaos-fear` 处理器，仅将同战场对手控制的【幸运】攻击威力设为0；新增1项回归测试。`npm test`：293 项 JavaScript/场景测试与 3 项 TypeScript 核心测试全部通过；`npm run validate`、`npm run audit:skills` 均通过。当前审计为 `FULL 123 / PARTIAL 821 / MANUAL 0 / DISABLED 0`。

## 当前里程碑：M2 / 基础对局与能力边界

- [x] TypeScript 领域状态与卡牌实例模型
- [x] 命令信封：`gameInstanceId`、`commandId`、`actorId`、`expectedRevision`
- [x] 阶段状态机：准备、前哨、行动、战斗
- [x] 跳过已淘汰玩家
- [x] 决策窗口可序列化、确认与取消
- [x] 败北状态的统一出牌拦截
- [x] 局势牌10移除2、剩余8张接3张高潮牌
- [x] 原版地图素材独立复制
- [x] 模式规则包注册接口
- [x] 标准与3X内置模式通过统一默认注册入口接入
- [x] 标准引擎连续推进准备、前哨、行动、战斗
- [x] 事件组随机选择与房间内固定事件牌堆
- [x] 电子版标准模式不自动创建实体规则 NPC；不足人数由联机/单机 AI 补位策略负责
- [x] 局势牌按结构化事件区配置放置
- [x] 深山町、新都战斗和事件战果
- [x] 侦察回合结束2战果，且每回合只结算一次
- [x] 战斗败北状态统一写入并拦截后续出牌
- [x] 应用门面、公开状态投影、快照恢复
- [x] 274项 JavaScript/场景测试和 3 项 TypeScript 核心测试全部通过

## 当前里程碑：M3 / 角色能力与模式规则包（基础运行时已开始）

### 最近阶段验证（2026-08-31）

- 本阶段依据开发版技能文本完成莱昂纳多·达·芬奇【口袋达·芬奇】的部署被动：仅在 `player.deployed` 事件将本人部署到魔术工房时获得1点魔力，普通移动事件不会触发。新增内容映射断言与端到端回归测试。`npm test`：274项 JavaScript/场景测试与3项 TypeScript核心测试全部通过；`npm run validate`、`npm run audit:skills` 和 `git diff --check` 均通过。后续批次审计覆盖为 `FULL 107 / PARTIAL 837`。

- [x] 被动与被动/阶段能力在运行时类型上分离
- [x] 技能卡显式魔力门槛作为使用前置条件校验
- [x] 技能每局/每回合使用限制的结构化记录与校验
- [x] 每局限一次卡牌在回合结束时移出游戏
- [x] 真名解放/真名隐藏作为结构化技能与卡牌使用条件
- [x] 真名解放语义拆分：使用前置条件、明置打出触发、可选阶段能力发动触发互不混用
- [x] 220张以独立【真名解放】词条开头的技能完成结构化迁移；正文仅提及词条的28张不会误标
- [x] 成功常规出牌为每张牌生成 `card.played` 事实事件，记录明暗、实例、实际支付和公开属性
- [x] 暗置牌事件不泄露定义；非法或支付失败的出牌事务不改变真名状态
- [x] 真名解放后公开从者身份和从者技能牌，仍独立隐藏御主身份
- [x] 卫宫、克洛伊【无限剑制】打出后解放真名但不要求预先解放；卫宫Alter同名牌不触发解放
- [x] 战斗阶段归零属性能力仅在同战场存在对应属性明置攻击时开放
- [x] 战斗结算拆分为战力快照、响应窗口和最终奖励三个阶段
- [x] 新增 `post-power-response` 阶段步骤与 `combat.response.complete` 命令
- [x] 12张同文【气息遮断】迁移为严格第二名、至少三名参与者、每回合一次的响应能力
- [x] 【气息遮断】可令战力最高的所有交战对手败北；两人战斗和存在中间战力时不开放
- [x] 待结算战斗快照接入状态不变量与存档恢复校验
- [ ] 被动触发器与阶段能力处理器逐项迁移（当前已完成 99 项，剩余能力保持 PARTIAL/MANUAL）
- [x] 第一批具体技能处理器：库·丘林/迪卢木多「战斗续行」
- [x] 第二批具体技能处理器：玄奘三藏「神性〔金蝉子〕」
- [x] 技能决策窗口与取消后的继续结算
- [x] 提亚马特衍生牌作为真实卡牌实例进入技能区并可当回合出牌
- [x] 残留牌跨回合保持激活；波涛之兽减少下一次常规出牌数量
- [x] 原始之龙/魔性之猪的基础战斗触发与战败关闭
- [x] 波涛之兽行动能力作为规则命令执行免费移动
- [x] 卡牌属性结构化读取，兼容旧版 typeLabel
- [x] 局势牌属性禁止规则接入出牌合法性检查
- [x] 基础攻击标记与原始之龙的基础攻击禁用规则
- [x] 旧版内容导入器保留基础攻击标记
- [x] “加入攻击”与“常规出牌”拆分为独立规则入口
- [x] 效果加入攻击同样记录每局限一次，防止重复生成或重复使用
- [x] 临时攻击在回合结束时移除，优先于残留状态清理
- [x] 关闭卡牌统一处理技能区、弃牌区和残留状态
- [x] 波涛之兽行动能力限制为从深山町或新都发动
- [x] 卡牌阶段能力通过独立 CardAbilityRegistry 注册，不再写死在标准引擎
- [x] CardAbilityRegistry 提供能力存在性查询，避免重复注册并支持内容包注入
- [x] 常规出牌与效果追加出牌共用卡牌进入攻击区校验组件
- [x] 技能使用次数统一由 usage-limits 组件计算与记录
- [x] 被动触发器按事件类型建立索引，避免无关能力全量扫描
- [x] 效果运行时提供处理器查询与排空上限，阻止无限效果循环
- [x] 快照恢复与应用命令边界接入状态不变量校验
- [x] 事件组事件卡引用、重复 ID 和结构形状校验
- [x] 费用计算与魔力支付统一由 costs 组件处理
- [x] 战斗威力和明置牌属性计算抽为纯规则组件
- [x] 局势牌持续战力修正：sit4-sit10 的属性加成、共同属性合计加成与地点范围结构化接入
- [x] 属性同义词规范化：敏捷/迅捷、魔法/魔术统一为规则 canonical 名称
- [x] 卡牌关闭后的技能区、弃牌区、移出游戏归属统一由生命周期组件计算
- [x] 3X 模式状态在创建对局时建立独立阶段和玩家预算
- [x] 3X 御主评级、初始圣晶石和购点结束清理
- [x] 3X 禁用/御主选择的唯一占用校验
- [x] 卡牌关闭后的生命周期归属统一处理
- [x] 回放事件 ID 唯一性和连续修订校验
- [x] 旧快照恢复补齐杰基尔/海德 `form` 字段，并拒绝非法形态或错误从者归属
- [x] 联机命令信封的空身份、非法修订和房主权限校验
- [x] 命令信封统一校验身份、房间和修订号
- [x] 回放元数据与事件修订号校验
- [x] 3X Ban/抽取/购点/从者选择/顺位的状态阶段结构
- [x] 联机快照消息的房间、版本和状态一致性校验
- [x] 公开投影决策窗口的参与者可见性测试
- [x] 标准内容包按已选从者 ID 初始化牌库，并保留按玩家 ID 的测试牌库兼容回退
- [x] 内容导入器在加载阶段校验从者牌库卡牌引用，拒绝未知卡牌定义
- [x] 正式 `DeckDefinition` 与 `CardDefinition` 分离，重复卡以 `definitionId + count` 保存
- [x] 开局优先展开正式牌库定义为独立卡牌实例，旧 `playerDecks` 仅保留兼容入口
- [x] 作者内容入口校验从者牌库必须为12张合法卡牌 ID
- [x] 目标卡临时规则修正：按稳定 definitionId 覆盖费用、8魔力门槛和局势属性限制
- [x] 临时规则修正支持回合、整局及来源牌激活期间三种生命周期，并接入快照不变量
- [x] 模式注册表提供版本校验、查询和枚举接口
- [x] 3X 圣晶石购买成本与重复购买递增规则抽为纯组件
- [x] 联机命令/快照传输消息定义与跨房间校验接口
- [x] 公开投影的卡牌、事件和牌堆可见性判断抽为独立组件
- [x] 局势属性禁止同时作用于攻击牌和对应阶段技能
- [x] 3X 准备状态保存稳定玩家名册并校验合法阶段转移
- [x] 3X 购点提交原子化，御主选定后按评级发放圣晶石
- [x] 3X 开局前校验每位玩家完成御主、从者和顺位选择
- [x] 3X 状态不变量接入通用状态校验，拒绝损坏快照
- [x] 联机重连握手与快照请求绑定房间、玩家身份和修订号
- [x] 3X 从者选择全员完成校验并将购点修正交接至开局玩家状态
- [x] 3X Ban、御主、购点、从者和顺位流程接入权威命令引擎
- [x] 3X Ban 阶段增加显式确认，防止单次点击隐式跳过阶段
- [x] 3X 购点结束按全员提交后才进入从者选择阶段
- [x] 3X 公开投影仅向本人展示圣晶石购点明细，隐藏其他玩家预算
- [x] 断线恢复拒绝不比本地更新的旧快照，防止状态回退
- [x] 命令信封拒绝空类型、空载荷和不存在的玩家身份
- [x] 卡牌能力执行拦截非对局状态与待决策期间的越权调用
- [x] 3X 购点确认后预算不可再次修改
- [x] 3X Ban 支持每位玩家独立提交并严格要求全员确认
- [x] 3X AI 先 Ban 使用确定性候选选择并拒绝重复禁用
- [x] 3X 从者候选池支持基础抽取与购点追加抽取，选择限制在本人候选内
- [x] 3X 御主候选池支持每人抽取三名并限制从本人候选中选择
- [x] 3X 内容池配置校验，拒绝空池、重复 ID 和非法条目
- [x] 3X AI 先 Ban 接入房主权威命令并向公开投影广播结果
- [x] 3X 高潮淘汰时应用购点提供的战果加成
- [x] 旧版 3X 快照自动迁移新增御主/从者候选字段
- [x] 内容导入器自动生成 3X 御主与从者候选池
- [x] 3X 候选抽取统一使用房主 RNG，并增加确定性重放测试
- [x] 3X 公开投影仅向本人展示御主/从者候选池
- [x] 3X 最终身份校验正式御主/从者内容池，防止绕过候选界面
- [x] 3X Ban 配置正式御主池后拒绝池外角色
- [x] 3X 候选状态不变量拒绝跨玩家重复占用
- [x] 卡牌能力统一拦截败北、淘汰、非持有者和已关闭卡牌
- [x] 3X 购点确认后锁定预算，禁止重复提交修改
- [x] 内容包集合、局势牌、事件组与文明废墟结构校验
- [x] 事件组固定20张且事件ID不可重复
- [x] 3X Ban、阶段结算、购点结算和顺位锁定的房主权限边界
- [x] 3X 购点项目白名单与失败原子性
- [x] 3X 预算修正值非负整数不变量
- [x] 文明废墟名称、战果、属性和文本字段结构校验
- [x] 决策窗口参与者、选项、选择范围和提交记录不变量校验
- [x] 效果队列 effectId 唯一性与无效帧拦截
- [x] 内容扩展包嵌套实体跨集合稳定 ID 冲突校验
- [x] 卡图确认拉克什米·芭伊牌库：12张基础牌含1张厄运（Misfortune）衍生牌
- [x] 技能定义支持等级、激活类型和阶段窗口结构校验
- [x] 卡牌实例每局/每回合/每阶段使用限制与回合清理统一接入
- [x] 卡牌能力入口按内容定义校验阶段窗口与实例使用限制
- [x] 卡牌使用限制标记接入状态不变量与快照恢复校验
- [x] 衍生卡统一实例工厂、所属区域挂接与来源 effectId 追踪
- [x] 衍生卡来源必须通过统一工厂提供非空 effectId，避免生成牌丢失溯源
- [x] 衍生卡批量工厂先完成全量校验再创建，失败不会留下半批实例
- [x] 衍生卡来源标记接入状态不变量与快照恢复校验
- [x] 五种卡牌属性及敏捷/迅捷、魔法/魔术同义词统一规范化
- [x] 结构化卡牌与局势禁用属性内容校验和导入规范化
- [x] 技能支持等级导入映射：implemented/FULL、pending/PARTIAL、manual/MANUAL、disabled/DISABLED
- [x] 未完成技能不会出现在普通对局操作，DISABLED 技能明确拒绝执行
- [x] 技能执行失败时费用与使用记录原子回滚
- [x] 技能注册表拒绝未知支持等级
- [x] 技能导入保留结构化属性与 handlerId，避免运行时解析展示文本
- [x] 结构化属性字段优先于展示标签，显式空属性不会错误回退
- [x] 卡牌与技能支持结构化阶段步骤窗口，并在合法性入口统一校验
- [x] 技能规则来源引用：英文版从者精确映射自动写入 `sourceRefs`，并校验 CHM 分类/页面一致性
- [x] 电子版 AI 玩家控制层基础：仅通过公开投影、自身私有区域和标准命令规划，不读取对手隐藏信息
- [x] 3X 独立模式规则包：设置阶段动作、阶段计划、公开设置投影和高潮购点胜利判定均实现为 GameModeDefinition
- [x] 标准模式独立规则包：3至7人边界、标准阶段计划、公开模式投影与普通战果胜利判定均实现为 GameModeDefinition
- [x] AI 对局运行器：按规则层提供的合法动作串行提交标准命令，支持决策等待、轮次交接和有界停止，不在 AI 层猜测规则
- [x] 阶段步骤窗口与结构化属性优先级回归测试
- [x] 事件牌生命周期：地点揭示、移除、弃牌重洗和替换
- [x] 事件牌堆输入校验与回合/替换抽取原子性
- [x] 事件场上唯一性与可见性状态不变量
- [x] 多参与者决策逐人提交，全部完成后才恢复效果队列
- [x] 决策恢复点预提交参与者、选项和数量范围校验
- [x] 标准资源与抽牌效果处理器；抽牌使用权威 RNG 并支持弃牌堆重洗
- [x] 命令领域事件统一进入 PassiveRuntime，再由效果队列结算
- [x] `residual` 技能类型和回合线性动态费用规则
- [x] 12张同文【阵地建造】：动态费用、残留激活和部署工房后的1魔力/2战果
- [x] 14张同文【骑乘】：行动阶段每回合一次、与基础攻击同批打出时抽1张
- [x] 【骑乘】追加出牌：手牌至多3张、印刷基本威力不超过3、按打出费用原子支付
- [x] 【骑乘】追加牌逐卡生成 `card.played`，继续遵循技能卡8魔力、真名和属性限制
- [x] 金时两张【黄金冲击】：结构化无视8魔力门槛、无视局势属性禁用、打出后真名解放与每局一次移除
- [x] 肯尼斯【双重御主】：开局强制被动登记玩家技能牌8魔力豁免，统一接入出牌合法性入口
- [x] 韦伯【战略部署】：结构化支付1点魔力抽2张牌，并限制在前哨阶段发动
- [x] 标准开局读取内容包中的御主初始魔力，未知御主按4点处理；3X开局仍由模式购点修正接管
- [x] 提亚马特【人类恶】：战斗结算后战胜她的每名玩家额外获得1点战果
- [x] 多能力卡结构：同一卡牌可声明独立阶段窗口、费用和使用限制，并按能力 ID生成操作
- [x] 12张【对魔力】：拆分【宝具绽放】与【魔术抗性】两个独立战斗效果；分别每回合一次
- [x] 【对魔力】能力门槛按规则区分：宝具绽放不要求技能卡激活，魔术抗性要求激活
- [x] 卡牌实例记录 `playedRound`，宝具绽放只统计本回合实际打出的宝具
- [x] 开局随机事件组写入固定事件池，后续回合不会混入其他事件组
- [x] `playedRound` 接入状态不变量与快照回归校验
- [ ] 其他衍生牌、宝具/特殊属性等卡牌实例规则
- [x] 杰基尔/海德形态限制：杰基尔形态禁止结构化【狂战士】牌，海德形态禁止【气息遮断】
- [x] 第8至11回合高潮淘汰与第11回合最高战果胜利判定
- [x] 3X 模式独立规则包（禁用、AI、圣晶石）
- [ ] PeerJS 房主权威传输与断线重连
- [ ] 新 UI 接入公开投影，不回调旧全局状态

旧版 `index.html`、`SkillLib.js` 和旧全局 `State` 不属于 V2 运行时，待对应规则通过测试后才会逐步退出迁移参考范围。

## 最近阶段验证（2026-08-31）

- 新增两项面向剩余技能的共享领域机制。其一是卡牌附着区：支持把自己或其他玩家的实体牌放在另一张牌上/牌下，保持所有权与来源，拒绝自引用和附着环，并可安全解除至合法区域；为【封印】【魔卡皮套】【分割思考】【提升等级】等效果提供统一基础。其二是计划效果：按领域事件、指定回合和到期回合序列化延迟结算，可单次或持续触发、取消、过期清理并进入统一效果队列；为“下回合”“战斗阶段结束后”“回合结束时”等132项待迁移技能提供统一基础。两组各新增3项回归测试，完整 `npm test` 当前为372项 JavaScript/场景测试与3项 TypeScript核心测试通过。
- 已将基础规则【墨水修订1版】、FQA、桌游问题解答、玩家回合流程和关键词、3X模式规则建立为只读、可搜索的 UTF-8 规则来源索引，共 700 条带文件及段落/表格/工作表定位的记录。新增规则来源提取器、搜索入口和2项索引回归测试；原始 Word、Excel 和文本资料不被修改。后续技能歧义必须通过该索引核对并保留定位，不再依赖临时人工翻阅或旧代码推断。

- 本批次依据基础规则书、FQA、关键词和开发版技能文本，登记19项明确“魔力少于8点也可打出/使用此牌”的结构化门槛例外。仅写入目标卡的 `requiresEightMana: false`，未将“允许另一张牌低于8点”误绑定到来源技能；附加阶段效果仍保持 `PARTIAL`，未伪装为 FULL。新增1项内容回归测试。`npm test`：278项 JavaScript/场景测试与3项 TypeScript核心测试通过；`npm run validate` 与 `npm run audit:skills` 均通过。当前审计为静态943、运行时944，FULL 107、PARTIAL 837、MANUAL 0、DISABLED 0。

- 本阶段依据开发版相同规则文本完成梅柳齐娜与阿尔比恩【龙之心】战斗结算后被动：仅当本人获胜，且同战场对手存在与己方某张攻击同属性、单张最终威力更高的攻击时，战斗奖励已结算后失去3点战果。战斗快照新增逐张最终威力与属性，处理器通过结构化事件判断，不解析展示名称；新增1项正向及1项无触发回归场景。`npm test`：273项 JavaScript/场景测试与3项 TypeScript核心测试全部通过；`npm run validate`、`npm run audit:skills` 和 `git diff --check` 均通过，技能覆盖为 FULL 99、PARTIAL 845。

- 本阶段全面迁移言峰绮礼【执行者】战斗阶段能力：使用时为本人本回合战斗总威力增加2点，效果通过结构化 `combatPowerBonus` 和统一 `roundPowerBonus` 战斗计算接入。新增1项回归测试；`npm test`：272项 JavaScript/场景测试与3项 TypeScript核心测试通过；`npm run validate`、`npm run audit:skills` 和 `git diff --check` 均通过，技能覆盖为 FULL 97、PARTIAL 847。

- 本阶段全面迁移一项规则明确的主技能被动：间桐慎二【小丑】监听 `combat.resolved`，在其战败状态写入后自动失去1枚令咒，令咒为0时保持为0。通过被动运行时和统一战斗事件接入，不依赖 UI 或技能名称判断。新增1项回归测试；`npm test`：271项 JavaScript/场景测试与3项 TypeScript核心测试通过；`npm run validate`、`npm run audit:skills` 均通过，技能覆盖为 FULL 96、PARTIAL 848。

- 本阶段补充戈耳工被动的跨回合状态清理：新回合开始时移除上一回合 `gorgon-np:*` 标记，保留其他模式状态；新增1项回归测试。`npm test`：270项 JavaScript/场景测试与3项 TypeScript核心测试通过；`npm run validate`、`npm run audit:skills` 均通过，技能覆盖为 FULL 95、PARTIAL 849。

- 本阶段依据开发版技能描述迁移戈耳工【石化魔眼】的强制被动：每回合首次由其他玩家打出宝具时触发；存活玩家不超过5人获得2点魔力，否则获得1点魔力。触发通过 `card.played` 领域事件和统一效果队列注册，并以回合标记保证只结算一次。新增2项回归测试；`npm test`：269项 JavaScript/场景测试与3项 TypeScript核心测试通过；`npm run validate`、`npm run audit:skills` 均通过，技能覆盖为 FULL 95、PARTIAL 849。

- 本阶段补强来源绑定修正的生命周期：回合结束关闭非残留来源后，自动移除 `while-source-active` 修正，避免其泄漏到后续回合；新增1项回归测试。`npm test`：267项 JavaScript/场景测试与3项 TypeScript核心测试通过；`npm run validate`、`npm run audit:skills` 均通过，技能覆盖为 FULL 94、PARTIAL 850。

- 本阶段将标准回合结束流程重构为基础规则明确的六步流水线：先结算战斗后效果，再移除御主、弃置局势与事件、处理残留及次数限制牌、关闭其余攻击牌，最后结算回合结束效果。新增可选阶段 hook 以供效果队列和模式层接入，保留旧调用兼容性；新增1项顺序回归测试。`npm test`：266项 JavaScript/场景测试与3项 TypeScript核心测试通过；`npm run validate` 与 `npm run audit:skills` 均通过（静态943、运行时944，FULL 94、PARTIAL 850、MANUAL 0、DISABLED 0）。

- 本阶段依据开发版文本完成爱丽丝菲尔【转换魔术】：前哨阶段将全部手牌弃置，并按弃置张数获得等量魔力；无手牌时不生成可用操作，费用、回合限制和弃牌归属均经端到端测试。新增2项测试；`npm test`：265项 JavaScript/场景测试与3项 TypeScript核心测试通过。技能覆盖更新为 FULL 94、PARTIAL 850。
- 本阶段依据开发版【黑泥】卡图完整迁移间桐樱的准备阶段能力：支付2点魔力，在【被污染的圣杯】未明置激活时获得2点战果；圣杯已激活、魔力不足或本回合已经使用时均由统一技能合法性与费用事务拦截。新增2项端到端测试；`npm test`：263项 JavaScript/场景测试与3项 TypeScript核心测试通过。技能覆盖更新为 FULL 93、PARTIAL 851。
- 本阶段依据开发版齐格御主卡与【幻想大剑·天魔失坠】卡图完整迁移【龙告令咒】：仅在本人行动阶段出牌步骤开放，消耗1枚令咒、获得2点魔力，通过统一衍生牌工厂将临时【幻想大剑·天魔失坠】加入御主技能区，并只对该稳定卡牌 ID 提供本回合8魔力门槛豁免；正常打出仍支付印刷4点魔力。回合清理扩展到手牌、牌库、弃牌区及双方技能区，未打出的临时牌也会移出游戏。新增3项端到端回归测试；`npm test`：261项 JavaScript/场景测试与3项 TypeScript核心测试通过。技能覆盖更新为 FULL 92、PARTIAL 852。【幻想大剑·天魔失坠】自身的免费打出及对手获得反制能力仍保持 `PARTIAL`，未伪装完成。

- 本阶段完成持续状态对目标卡的结构化临时规则修正：新增 `CardRuleModifier`，按稳定 `definitionId` 定位目标，可独立覆盖费用、8魔力门槛和局势属性限制；支持回合、整局、来源牌激活期间三种生命周期。实际常规出牌、效果追加出牌和费用支付均读取同一修正结果，回合修正自动清理，旧快照自动补空数组，损坏来源、费用、目标和重复 ID 由状态不变量拒绝。该机制为巴泽特【第二天】及齐格【龙告令咒】等目标卡修正提供结构化基础。新增 4 项回归测试；`npm test`：258 项 JavaScript/场景测试与 3 项 TypeScript 核心测试通过；`npm run validate`、`npm run audit:skills` 和 `git diff --check` 均通过。

- 本阶段完成正式牌库定义迁移：新增独立 `DeckDefinition`，旧有序复数牌库在内容导入时压缩为 `definitionId + count`，开局再展开为具有不同实例 ID 的 `CardInstance`；牌库定义校验归属、正整数计数、重复条目及卡牌定义引用。标准内容优先使用正式牌库定义，旧 `playerDecks` 仅保留小型测试/旧作者包兼容。作者内容入口同时要求从者牌库为数组、恰好12张且卡牌 ID 合法。新增3项回归测试；三轮 `npm test` 依次为252、253、254项 JavaScript/场景测试通过，3项 TypeScript核心测试通过。
- 用户最新裁定：御主和从者都以 `Fate_Domination-开发版` 当前实际存在的角色、牌库、技能区及卡图为迁移范围，开发版中的全部从者均需迁移；不再用 CHM 的英文版/中文版目录筛选角色。开发版缺图或无法确认卡牌归属时才查 CHM 图鉴，开发版旧代码行为仍不得作为规则依据。
- 用户最新裁定：后续技能效果优先只读核对 `Fate_Domination-开发版` 中的对应卡图，不再要求每项都从 CHM 图鉴取图；CHM 保留为缺图、辨认失败和版本归属核对的备用来源。开发版旧代码仍不得作为规则依据。
- 本阶段完成旧快照形态字段兼容：`restoreSnapshot` 将历史玩家缺失的 `form` 迁移为 `null`，状态不变量拒绝未知形态以及非杰基尔玩家携带形态；新增 2 项快照回归测试。`npm test`：229 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。
- 本阶段未升级其他角色技能支持等级；杰基尔【气息遮断】已依据开发版卡图接入 `core.presence-concealment`，海德禁用由共享合法性入口执行。

- 本阶段完成技能来源追溯元数据：英文版图鉴索引 181 个精确映射对应 590 个 CHM 来源，另有 353 个未核对技能明确标记为 `legacy` 迁移来源，共 943 个静态技能均可追溯；运行时仍只使用稳定 ID 与结构化字段，不解析描述文本。新增来源格式校验和覆盖回归测试。
- 本阶段新增 AI 玩家控制层：`projectAiState` 只提供公开投影加 AI 自身私有区域，`conservativeAiPolicy` 只选择明确合法技能或可取消决策，不猜牌、不越权、不自动解释技能文本。新增 5 项 AI 隔离与命令规划回归测试。
- 本阶段补充 AI 对局运行器：`runAiUntilBlocked` 只调用外部合法动作发现和标准 dispatch，使用确定性命令 ID，遇到他人决策、轮次交接、策略放弃或无进展循环会停止。新增 3 项运行器回归测试；`npm test`：210 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。
- 本阶段补充衍生卡来源工厂：生命之海生成的魔兽改用强制来源 API，普通工厂也拒绝空 `createdByEffectId`。新增 1 项回归测试；`npm test`：211 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。`npm run validate` 与 `npm run audit:skills` 均通过；审计仍为静态943、运行时944，FULL 87、PARTIAL 857、MANUAL 0、DISABLED 0。
- 本阶段补充 3X 独立模式规则包：createThreeXModeDefinition 复用现有 3X 状态与经济规则，提供 3～7 人限制、标准设置命令动作、无隐藏信息的公开设置投影和高潮购点修正胜利判定。新增 3 项回归测试；npm test：214 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。npm run validate 与 npm run audit:skills 均通过。
- 本阶段补充统一默认模式注册入口：`createDefaultModeRegistry` 在不修改通用状态机的情况下注册标准与 3X 两个不可变规则包，并验证配置隔离。新增 2 项回归测试；`npm test`：219 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过；`npm run validate`：通过（4 个地点、68 名御主、183 名从者，4 条英文版从者来源警告）；`npm run audit:skills`：通过（静态 943、运行时 944，FULL 87、PARTIAL 857、MANUAL 0、DISABLED 0）。
- 本阶段修复内容包牌库键错误：正式从者牌库按已选 `servantId` 读取，旧测试/作者包仍可按玩家 ID 回退。新增 1 项开局回归测试；`npm test`：221 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。
- 本阶段补充衍生卡批量原子工厂 `createDerivedCardInstances`，统一处理多张生成牌的来源、实例 ID 和失败回滚边界。新增 1 项回归测试；`npm test`：222 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。
- 本阶段补充从者牌库引用校验，未知卡牌在内容导入阶段即被拒绝。新增 1 项回归测试；`npm test`：223 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。

- 本阶段根据电子版裁定移除固定 NPC 规则及其运行时分支；实体规则书中的 NPC 条款仅作为来源记录，不在电子版标准对局自动创建。后续不足人数由独立 AI 补位策略处理，不改变玩家状态模型。
- 保留并完成 3X 规则书御主评级映射，未列出的御主按规则使用默认 4 石；`npm test`：197/197 项 JavaScript/场景测试通过；3/3 项 TypeScript 核心测试通过。
- `npm run validate`：通过；4个地点、68名御主、183名从者。
- `npm run audit:skills`：静态943、运行时944；FULL 87、PARTIAL 857、MANUAL 0、DISABLED 0。
- 技能审计无 FULL 缺处理器、无非 FULL 绑定处理器；动态运行时技能仅提亚马特【生命之海】。
- 本阶段补充电子版 NPC 排除回归，确认标准开局、回合推进、战力和玩家窗口均只处理真实玩家；前置阶段已完成归零属性合法性与目标属性结构化校验、阿拉什准备阶段战果条件、高潮额外战果和喀戎战斗阶段自费加入攻击。
- 前置阶段已完成提亚马特【人类恶】、12 张 Saber 类【对魔力】多能力卡、3 张【十二试炼】及卫宫【炽天覆七重圆环】，均接入事件/效果队列或结构化战力修正。
- 下一批候选为其它有明确卡图和规则来源的战败响应或战斗结算后能力；需要先确认持久威力修正和奖励事件的统一记录方式。御主西苑的【对魔力 EX】和大批衍生牌仍需逐项证据，暂不批量套用。

- 本批次完成 3 张规则明确的纯牌库攻击技能迁移：御主葛木【蛇】、远坂凛【阴炁弹】、从者曼迪卡尔多【木剑】统一复用 `core.card-play`，并保留凛【阴炁弹】的每局一次限制。新增纯牌库攻击映射回归覆盖；`npm test`：230 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。`npm run validate`：通过（4 个地点、68 名御主、183 名从者）；`npm run audit:skills`：通过（静态 943、运行时 944，FULL 90、PARTIAL 854、MANUAL 0、DISABLED 0）。
- 本批次完成拉克什米·芭伊【厄运（Misfortune）】的战斗结算后卡牌响应：仅在本人战力低于最高者且手牌持有该牌时开放，弃置后获得3战果并令同战场其他对手败北；响应窗口与角色技能注册表解耦，未迁移角色技能的内容包也能正常开放卡牌响应。新增2项资格与完整结算回归测试；`npm test`：232 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。`npm run validate`：通过（4 个地点、68 名御主、183 名从者）；`npm run audit:skills`：通过（静态 943、运行时 944，FULL 90、PARTIAL 854、MANUAL 0、DISABLED 0）。
- 本阶段完成局势牌持续战力修正：sit4-sit6 在深山町/新都按力量、迅捷、魔术属性分别提供 +2；sit7-sit9 对应属性提供 +1；sit10 在至少两张明置激活攻击存在共同属性时提供合计 +3。规则运行时读取结构化 `combatPower`，不解析展示文本；新增3项回归测试。`npm test`：238 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过；`npm run validate`：通过（4 个地点、68 名御主、183 名从者）；`npm run audit:skills`：通过（静态 943、运行时 944，FULL 90、PARTIAL 854、MANUAL 0、DISABLED 0）。
- 本阶段完成事件牌堆防御性迁移：初始化拒绝重复事件 ID、非法战果和非法地点；事件放置数量必须为非负整数；标准回合开始在草稿状态中完成局势、资源、补牌与两地事件抽取，事件不足时不留下半回合状态；事件替换同样以草稿状态提交，并保留弃牌重洗规则；快照状态不变量拒绝同一事件同时存在于牌堆、弃牌堆或场上。新增 5 项事件牌回归测试；`npm test`：243 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。待阶段验证后记录内容与技能审计结果。
- 本阶段补齐牌库定义边界：牌库作为角色配置而非 `CardDefinition` 字段；内容导入拒绝非数组牌库与空卡牌 ID，继续拒绝未知卡牌引用，并允许同名牌的合法复数。新增 1 项牌库校验回归测试；`npm test`：244 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过。
- 本阶段完成杰基尔/海德形态限制与【气息遮断】内容注册：开发版卡图确认杰基尔禁止【狂战士】、海德禁止【气息遮断】，并确认杰基尔第三张技能为同规则的战力结算后响应。六张狂战士基础攻击通过稳定 ID 导入 `berserker-attack` 结构化标签，杰基尔【气息遮断】通过稳定 ID 接入 `core.presence-concealment`，统一入口按形态拦截，不读取卡牌名称；新增形态、内容注册、杰基尔可响应和海德不开放响应回归测试。`npm test`：251 项 JavaScript/场景测试通过，3 项 TypeScript 核心测试通过；`npm run validate` 与 `npm run audit:skills` 均通过；审计为静态 943、运行时 944，FULL 91、PARTIAL 853、MANUAL 0、DISABLED 0。
